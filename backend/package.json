{
  "name": "point-cloud-annotator-backend",
  "version": "1.0.0",
  "description": "Backend for Point Cloud Annotator - AWS Lambda + DynamoDB",
  "type": "module",
  "scripts": {
    "start": "node local-server.js",
    "dev": "node local-server.js",
    "deploy": "pnpm --dir src install && sam build && sam deploy --guided",
    "deploy:prod": "pnpm --dir src install && sam build && sam deploy"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "lowdb": "^5.1.0"
  }
}
